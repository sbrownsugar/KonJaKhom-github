# ก่อนจะค่อม (KonJaKhom) — แพ็กเกจสำหรับ "เดโมสาธารณะ" เท่านั้น
# =============================================================================
# ⚠️ ไฟล์นี้อยู่บน branch `demo` และต่างจาก branch `main` โดยตั้งใจ
#
#   main  = โค้ดที่ส่งประกวด (ตรงกับไฟล์ ZIP ที่ส่ง) -> มี mediapipe/opencv ครบ
#           เพราะมีกล้องและการวัดมุมท่าทาง   << จะรันของจริง ให้ใช้ branch main
#   demo  = เวอร์ชันสาธารณะบน Streamlit Cloud -> ปิดกล้อง (BZ_PUBLIC_DEMO=1)
#           จึงไม่ต้องมี mediapipe / opencv / av เลยแม้แต่ตัวเดียว
#
# ทำไมถอด mediapipe ออกแล้วระบบยังทำงานครบ:
#   น้ำหนัก (beta) ของมุมท่าทางทุกตัวในสมการความเสี่ยง = 0 ตามหลักฐาน
#   (Raine Study n=686 / Mahmoud 2019 meta / ResNet50 n=531 -> AUC 0.611)
#   -> ดัชนีความเสี่ยงคำนวณจาก "พฤติกรรม" ล้วน กล้องมีไว้วัดผลก่อน/หลังท่าบริหาร
#   -> เดโมนี้จึงเป็นหลักฐานเป็น ๆ ว่าคำเคลมนั้นจริง:
#      ไม่มี AI ติดตั้งอยู่บนเซิร์ฟเวอร์นี้เลย แต่ยังคัดกรองได้ครบทุกอย่าง
#
# ไม่ต้องมี packages.txt / libgl1 เพราะไม่มี opencv -> build เร็วและไม่พัง
#
# เรื่องเวอร์ชัน: ตั้งเป็นขั้นต่ำ (>=) ไม่ใช่ตรึง (==) โดยตั้งใจ
#   เครื่องที่พัฒนาใช้ Python 3.14 แต่เซิร์ฟเวอร์คลาวด์ใช้คนละรุ่น
#   การตรึงเวอร์ชันที่มีเฉพาะบน 3.14 (เช่น pandas==3.0.3) จะติดตั้งไม่ได้เลย
# =============================================================================

streamlit>=1.40
numpy>=1.26
pandas>=2.0
matplotlib>=3.8

# แดชบอร์ดครูเรียก analysis/run_analysis.py ซึ่งใช้ 2 ตัวนี้
scipy>=1.11
scikit-learn>=1.3
